#!/usr/bin/env node

const url = require('..').URL;

System.import(
	url.fromLocal(require('path').resolve(process.cwd(), process.argv[2]))
).catch(function(err) {
	console.error(err);
	process.exit(1);
});
